% !TeX root = proyecto.tex

% CAPÍTULO 6: DESARROLLO DE TRABAJO TERMINAL I

\chapter{Desarrollo del Trabajo Terminal I}
\label{cap:desarrollo_tt1}

En este capitulo se presenta una síntesis de los tres prototipos desarrollados durante
el Trabajo Terminal I (agosto - diciembre 2025). El propósito no es poner la misma
documentación técnica sino extraer las métricas clave, documentar las
decisiones más relevantes y sobretodo argumentar las limitaciones identificadas
que justifican la migración del enfoque utilizado. Cada prototipo es presentado como un
experimento pues su valor radica tanto en lo que logró como en lo que
demostró no ser viable. 


\section{Prototipo 0: Scraping directo sobre HTML}
\label{sec:p0_fracaso}


\subsection{Premisa y Enfoque}
\label{subsec:p0_premisa}

El Prototipo~0 se planteó como una prueba de concepto que fue verificar si era posible
extraer noticias directamente del HTML de los sitios de noticias mexicanas, utilizando
\texttt{BeautifulSoup} y la librería \texttt{requests}. El supuesto inicial era
que los sitios de noticias de acceso público serían accesibles de forma programática
sin fricción alguna.

\subsection{Resultado: Inviabilidad por seguridad perimetral}
\label{subsec:p0_resultado}

La premisa fue invalidada en su totalidad pues el sistema se encontró con dos barreras
técnicas alarmantes:

\begin{enumerate}
    \item \textbf{WAF y Cloudflare (HTTP 403/503).} Cerca del 78\% de los medios objetivo
          (incluyendo portales como \textit{Reforma}, \textit{El Universal} y \textit{Milenio})
          operaban detrás de un WAF de grado empresarial. Lo que hacia la librería \texttt{requests}
          era generar un hash TLS/JA3 determinístico que los sistemas de detección identificaban
          como un cliente automatizado, recibiendo como respuesta HTTP 403 (Forbidden).

    \item \textbf{Contenido dinámico renderizado en cliente.} Múltiples sitios
          entregan el marcado HTML del artículo mediante llamadas AJAX en JavaScript
          ejecutadas en el navegador. \texttt{BeautifulSoup} al trabajar sobre el
          HTML estático descargado solo encontraba el esqueleto de la página sin
          el contenido informativo lo que hacia imposible la extracción.
\end{enumerate}

\subsection{Decisión Técnica}
\label{subsec:p0_leccion}

El fracaso del prototipo 0 hizo evidente que el scraping al HTML directo no era una estrategia viable
para el monitoreo de medios de noticias modernos. Por lo que se decidió abandonar el scraping directo
y migrar hacia fuentes diseñadas para la ingestión automatizada, como lo son los feeds RSS y las APIs.

\section{Prototipo 1: RSS + K-Means}
\label{sec:p1_baseline}

\subsection{Arquitectura implementada}
\label{subsec:p1_arquitectura}

El Prototipo~1 estableció la primera iteración funcional del sistema. La migración hacia
feeds RSS eliminó la fricción de acceso, pues los feeds son recursos XML diseñados
explícitamente para el consumo automatizado y no están sujetos a los mecanismos WAF
que bloquearon el P0.

El flujo implementado consistió en las siguientes etapas:

\begin{enumerate}
    \item \textbf{Recolección.} Ingestión de 8 fuentes RSS nacionales mediante
          \texttt{feedparser}, extrayendo por cada \texttt{<item>} el título,
          descripción, enlace y fecha de publicación.
    \item \textbf{Detección rudimentaria de NNA.} Función \texttt{detect\_children\_mentions}
          con 15 palabras clave aisladas (\textit{``hijo''}, \textit{``menor''},
          \textit{``niño''}, \textit{``huérfano''}, etc.).
    \item \textbf{Vectorización TF-IDF.} Representación de documentos en
          $\mathbb{R}^{1000}$ mediante \texttt{TfidfVectorizer} con
          \texttt{max\_features=1000}, \texttt{ngram\_range=(1,1)}.
    \item \textbf{Agrupamiento K-Means.} Clustering con $k=4$ fijo y métrica
          euclidiana sobre la matriz TF-IDF.
    \item \textbf{Persistencia.} Exportación a CSV mediante \texttt{Pandas}.
\end{enumerate}

\textbf{Volumen de datos:} 96 noticias únicas por ejecución; persistencia en
archivos planos CSV.

\subsection{Limitaciones Identificadas}
\label{subsec:p1_limitaciones}

Las limitaciones del P1 fueron de tres órdenes:

\begin{description}
    \item[\textbf{Techo semántico del detector.}] La detección por palabras clave
          aisladas generó una tasa de falsos positivos del \textbf{40\%}: noticias
          sobre legislación (``aprobación de ley de protección al menor''), campañas
          de prevención y estadísticas de incidencia activaban el detector sin
          representar casos concretos de orfandad.

    \item[\textbf{Maldición de la dimensionalidad en K-Means.}] El algoritmo asume
          clústeres globulares de tamaño similar y requiere especificar $k$
          \textit{a priori}. Con 96 documentos y $k=4$, el sistema forzó eventos
          periodísticos distintos en el mismo clúster para balancear tamaños,
          generando grupos sin coherencia semántica real. El \textit{Silhouette
          Score} resultante fue de \textbf{0.23} ---calidad de agrupamiento
          inaceptable---. El vocabulario limitado a 1{,}000 características
          también excluía términos especializados del dominio como
          \textit{``custodia''}, \textit{``DIF''} y \textit{``tutor legal''}.

    \item[\textbf{Cobertura y escalabilidad.}] El sistema cubría solo medios
          nacionales con feed RSS público, excluyendo la prensa regional donde
          suelen aparecer los casos más específicos de feminicidios con NNA
          identificados.
\end{description}


\section{Prototipo 2: Triple Fuente + DBSCAN + FeminicideDetector}
\label{sec:p2_culminacion}


\subsection{Evolución del sistema}
\label{subsec:p2_evolucion}

El Prototipo~2 que fue la culminación del TT-1 reestructuró
el sistema en respuesta directa a las limitaciones del P1. Los cambios fueron
sustanciales en las tres capas del flujo de procesamiento:

\begin{description}
    \item[\textbf{Recolección: Arquitectura de Triple Fuente.}]
    Se amplió la estrategia de ingesta a tres mecanismos complementarios:
    (a)~\textbf{10 feeds RSS} nacionales y de género (incorporando CIMAC Noticias
    y Milenio, especializadas en violencia de género);
    (b)~\textbf{Google News API} con 5 consultas de dominio
    (\textit{``feminicidio México''}, \textit{``asesinato mujer''},
    \textit{``violencia género''}, etc.), con rate limiting de delays aleatorios
    2--5~s y checkpoints cada 30~peticiones para evitar HTTP~429;
    (c)~\textbf{Historical Scraper} mediante paginación mensual de Google News,
    cubriendo el período mayo--noviembre 2025 (6~meses de retrospectiva).

    \item[\textbf{Detección: Clase \texttt{FeminicideDetector} (72 patrones RegEx).}]
    Se reemplazó la función rudimentaria por una clase especializada con
    \textbf{tres categorías de patrones}: 40~patrones de feminicidio (incluyendo
    patrones contextuales como \texttt{r'mujer.*asesinada'} y
    \texttt{r'cadáver.*femenino'}), 15~patrones de NNA (patrones relacionales
    como \texttt{r'hijo.*huérfano'} y \texttt{r'adolescente.*perdió.*madre'})
    y \textbf{17~patrones de exclusión}. Esta última categoría fue la innovación
    crítica del P2: al detectar términos como \texttt{r'ley.*protección'},
    \texttt{r'estadística'} o \texttt{r'campaña.*prevención'}, la noticia era
    descartada automáticamente antes de alcanzar el clasificador, resolviendo el
    principal fallo del P1. El sistema asignaba además un \textit{score} de
    confianza continuo y una prioridad discreta (Alta/Media/Baja/Irrelevante).

    \item[\textbf{Agrupamiento: DBSCAN en lugar de K-Means.}]
    El cambio de K-Means a DBSCAN (\texttt{eps=0.8}, \texttt{min\_samples=2},
    \texttt{metric='cosine'}) responde a las propiedades de la distribución de
    noticias periodísticas: la mayoría de los feminicidios son eventos únicos
    con cobertura mínima (1--2~noticias), mientras que un evento mediáticamente
    relevante (como el caso Ángela Birkenbach) genera una densidad local de
    5--10~noticias. DBSCAN identifica naturalmente estas densidades sin
    requerir $k$ \textit{a priori}, asignando como ruido (\texttt{topic\_id=-1})
    los eventos genuinamente únicos, que precisamente son los más valiosos para
    el sistema: representan casos individuales no redundantes.
\end{description}

%----------------------------------------------------------------------------------------
\section{Métricas Baseline del TT-1}
\label{sec:metricas_baseline}
%----------------------------------------------------------------------------------------

La Tabla~\ref{tab:baseline_tt1} consolida los resultados cuantitativos de la
ejecución de referencia del Prototipo~2, realizada el 18 de noviembre de 2025
(datos almacenados en \texttt{data/reporte\_ml.json} y
\texttt{data/clusters\_info.csv}), que constituyen la \textbf{línea base oficial}
contra la cual se evalúan las mejoras del TT-2.

\begin{table}[H]
\centering
\caption{Métricas Baseline del TT-1 (Prototipo 2, 18 Nov 2025)}
\label{tab:baseline_tt1}
\renewcommand{\arraystretch}{1.5}
\begin{tabular}{|p{5.0cm}|p{3.5cm}|p{5.0cm}|}
\hline
\rowcolor{black}
\textcolor{white}{\textbf{Métrica}} &
\textcolor{white}{\textbf{Valor (P2)}} &
\textcolor{white}{\textbf{Contexto}} \\
\hline
\textbf{Noticias raw recolectadas} &
470 &
RSS (72) + Google News (148) + Historical Scraper (250) \\
\hline
\textbf{Noticias únicas (post-dedup)} &
281 &
40.2\% de duplicados detectados (189); threshold coseno 0.75 \\
\hline
\textbf{Noticias clasificadas como objetivo} &
52 (18.5\%) &
Superan criterio doble: feminicidio confirmado + mención de NNA \\
\hline
\textbf{Distribución de prioridad} &
Alta: 5 (1.8\%) \newline Media: 11 (3.9\%) \newline Baja: 36 (12.8\%) &
Basada en el score de confianza del \texttt{FeminicideDetector} \\
\hline
\textbf{Tasa de falsos positivos (P2)} &
\textbf{10\%} &
Validación manual de muestra aleatoria de 20 noticias; 2/20 FP \\
\hline
\textbf{Tasa de falsos positivos (P1)} &
40\% &
Reducción del 75\% lograda mediante los 17 patrones de exclusión \\
\hline
\textbf{Clústeres DBSCAN identificados} &
3 &
Clúster~0: estadísticas REDIM (7 noticias); Clúster~1: Caso Ángela Birkenbach (5); Clúster~2: huérfanos Animal Político (3) \\
\hline
\textbf{Outliers DBSCAN} &
199 (81.4\%) &
Noticias de casos únicos; comportamiento esperado y semánticamente correcto \\
\hline
\textbf{Silhouette Score --- P1 (K-Means)} &
0.23 &
Calidad de agrupamiento inaceptable; $k=4$ forzado arbitrariamente \\
\hline
\textbf{Silhouette Score --- P2 (DBSCAN)} &
\textbf{0.48} &
Calculado solo sobre los 3 clústeres densos; mejora del +109\% \\
\hline
\textbf{Cobertura temporal} &
6 meses &
Mayo--noviembre 2025; insuficiente para análisis de tendencias \\
\hline
\textbf{Entidades extraídas (NER)} &
0 &
Ausencia total; análisis estructurado requería lectura humana \\
\hline
\end{tabular}
\end{table}


\section{Limitaciones identificadas}
\label{sec:limitaciones_motivacion}


Las métricas no solo validan la viabilidad técnica del proyecto sino que también delimitan
las condiciones de frontera que hacen que el sistema del P2 sea insuficiente para
su adopción operativa por organizaciones civiles.

\subsection{La barrera del 80\% de falsos positivos: Insuficiencia de la Sintaxis}
\label{subsec:limit_fp}

El \texttt{FeminicideDetector} redujo los falsos positivos del 40\% (P1) al 10\%
(P2), un avance medible e importante. Sin embargo, el análisis cualitativo de los
2~falsos positivos en la muestra de 20~noticias revela un patrón que no puede
resolverse añadiendo más expresiones regulares:

\begin{itemize}
    \item \textit{``Durango aprueba la Ley Nicole para prohibir cirugías estéticas
          en menores de edad''}: La noticia activa el patrón de NNA (\textit{``menores
          de edad''}) sin que los patrones de exclusión capturaran el término
          \textit{``aprueba''} en ese contexto legislativo específico.
    \item \textit{``REDIM reporta: 1 de cada 4 NNA vive en situación de violencia
          familiar''}: El término \textit{``NNA''} y \textit{``violencia''} activan el
          detector en una nota estadística sin ningún caso concreto identificado.
\end{itemize}

El problema de fondo no es la cantidad de patrones ---el sistema ya tenía 72--- sino
la \textbf{naturaleza sintáctica} del enfoque: la co-ocurrencia de términos no
implica la co-ocurrencia de los \textit{roles semánticos} que esos términos desempeñan
en la oración. Una expresión regular no puede distinguir si \textit{``menor''} en
una noticia es la víctima directa (una menor asesinada), la víctima indirecta (el
hijo huérfano de la madre asesinada), o el sujeto de una campaña legislativa.
Esta distinción es esencial para el dominio del sistema, y es exactamente el tipo de
comprensión que los modelos de lenguaje bidireccionales como BETO proveen a través de
sus mecanismos de atención: la representación de cada token es función del contexto
\textit{completo} de la oración.

\subsection{El Techo del Clustering Léxico: DBSCAN sobre TF-IDF}
\label{subsec:limit_clustering}

Los 3~clústeres identificados por DBSCAN son semánticamente coherentes, pero el
81.4\% de outliers ---aunque correcto para eventos genuinamente únicos--- oculta
un problema metodológico más profundo: noticias sobre el \textit{mismo evento
fáctico} redactadas con vocabulario diferente por distintos medios no son agrupadas.

Ejemplo documentado: \textit{``Mamá asesinada deja 2 hijos huérfanos en Ecatepec''
(nota policiaca, \texttt{El Sol de México})} y
\textit{``Feminicidio en el Estado de México: dos menores quedan sin madre''
(nota de derechos humanos, \texttt{CIMAC Noticias})} reportan el mismo hecho con
vocabularios disjuntos. Los vectores TF-IDF de ambas noticias tienen similitud
coseno de 0.18 (muy por debajo del umbral eps=0.8), por lo que DBSCAN los clasifica
como documentos independientes. En el espacio semántico de BETO, ambos documentos
tendrían embeddings con similitud coseno superior a 0.85, siendo correctamente
agrupados como instancias del mismo evento.

\subsection{La Inescalabilidad del Almacenamiento en CSV}
\label{subsec:limit_csv}

La persistencia en archivos CSV impone restricciones estructurales que no son
atributos del formato sino consecuencias de él:

\begin{itemize}
    \item \textbf{Ausencia de integridad referencial.} No existe mecanismo que
          garantice que una detección apunte a una noticia existente, ni que
          un clúster referencie documentos válidos.
    \item \textbf{Sin acceso concurrente.} La escritura por el Worker y la
          lectura por el dashboard desde el mismo archivo CSV introduce condiciones
          de carrera; el P2 las mitigó mediante escritura atómica por renombrado,
          pero no las eliminó.
    \item \textbf{Consultas de complejidad $O(n)$.}  Filtrar las noticias de
          prioridad Alta en el Estado de México entre junio y agosto de 2025
          requería recorrer el CSV completo en memoria.
\end{itemize}

\subsection{Síntesis: La Necesidad de Comprensión Semántica}
\label{subsec:limit_sintesis}

Las tres limitaciones anteriores convergen en una conclusión arquitectónica
unificada: el problema de la identificación de NNA en situación de orfandad por
feminicidio \textbf{requiere comprensión semántica del lenguaje natural}, no solo
reconocimiento de patrones léxicos. El texto de una noticia periodística es un
artefacto pragmático donde el significado emerge de la interacción entre entidades,
roles semánticos, modalidades y contexto narrativo ---dimensiones que están
fundamentalmente fuera del alcance de las expresiones regulares.

La decisión de migrar al paradigma neuro-simbólico del TT-2 no es una preferencia
por la novedad tecnológica, sino la respuesta \textit{necesaria} a una limitación
medida empíricamente: con el 10\% de falsos positivos documentado, un analista que
revisara las 52~noticias clasificadas como objetivo encontraría 5~falsas alarmas;
a escala de un sistema procesando 1{,}000~noticias diarias, esa tasa generaría
$\sim$100~registros espurios al día, erosionando la confianza operativa del
dashboard y la calidad del corpus.

La Tabla~\ref{tab:baseline_tt2_meta} formaliza los objetivos cuantitativos que el
TT-2 debe alcanzar para superar el baseline establecido por el TT-1.

\begin{table}[H]
\centering
\caption{Comparativa de Métricas: TT-1 (Baseline) vs. Metas del TT-2}
\label{tab:baseline_tt2_meta}
\renewcommand{\arraystretch}{1.4}
\begin{tabular}{|p{4.5cm}|c|c|c|}
\hline
\rowcolor{black}
\textcolor{white}{\textbf{Métrica}} &
\textcolor{white}{\textbf{TT-1 (P2)}} &
\textcolor{white}{\textbf{Meta TT-2 (P4)}} &
\textcolor{white}{\textbf{Mejora}} \\
\hline
\textbf{Falsos positivos} & 10\% & $\leq$5\% & $-50\%$ \\
\hline
\textbf{Fuentes monitoreadas} & 10 RSS + API & 45 RSS + API + Stealth & $+350\%$ \\
\hline
\textbf{Silhouette Score} & 0.48 & $\geq$0.60 & $+25\%$ \\
\hline
\textbf{Outliers de clustering} & 81.4\% & $\leq$55\% & $-32\%$ \\
\hline
\textbf{Cobertura temporal} & 6 meses & Continua (scheduler) & Operacional \\
\hline
\textbf{Persistencia} & CSV (plano) & PostgreSQL 16 (ACID) & Estructural \\
\hline
\textbf{Latencia de consulta} & $O(n)$ scan CSV & $O(\log n)$ índice GIN & Órdenes de magnitud \\
\hline
\textbf{Motor de clasificación} & RegEx (72 patrones) & BETO + Neuro-Simbólico & Paradigma distinto \\
\hline
\end{tabular}
\end{table}

Las metas de la Tabla~\ref{tab:baseline_tt2_meta} no son aspiraciones optimistas:
son los umbrales mínimos que hacen del sistema una herramienta operativamente
confiable para organizaciones civiles que trabajan con casos reales de NNA en
situación de vulnerabilidad.
